const graphVizPrintHelper = {
  pre() {
    console.log("Digraph G{")
  },

  per(parent, ...children) {
    for (let child of children) {
      const label = (parseInt(parent.value) > parseInt(child)) ? "L" : "R"
      if (child !== "NULL") console.log(`${ ' '.repeat(2) }"${parent.value}" -> "${child}"[label=${label}]`)
    }
  },

  post(obj) {
    console.log(`
    }`)
  }
}

module.exports = {
  graphVizPrintHelper
}